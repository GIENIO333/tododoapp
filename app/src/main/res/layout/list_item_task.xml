<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="@dimen/task_item_padding">

    <ImageView
        android:id="@+id/task_item_icon"
        android:layout_width="@dimen/task_item_icon_size"
        android:layout_height="@dimen/task_item_icon_size"
        android:contentDescription="@string/task_category_icon"
        android:scaleType="centerInside"
        android:layout_marginEnd="@dimen/task_item_content_margin" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/task_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/task_item_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/task_item_content_margin"
            android:textColor="@android:color/darker_gray"
            android:textSize="14sp" />
    </LinearLayout>

    <CheckBox
        android:id="@+id/task_item_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/task_item_content_margin"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text=""
        android:contentDescription="@string/done_label" />

</LinearLayout>